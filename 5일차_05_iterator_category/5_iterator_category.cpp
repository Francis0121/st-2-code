//5_iterator_category
#include <iostream>
#include <list>
#include <algorithm> // find() 같은 함수가 여기 있습니다.
using namespace std;

int main() {
	list<int> s;
	s.push_back(10);
	s.push_back(20);

	// STL 함수들 : 다양한 타입에 적용되는 일반화 함수
}